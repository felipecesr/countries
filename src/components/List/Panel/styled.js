import styled from 'styled-components';

export const PanelWrapper = styled.li`
  background-color: var(--color-elements);
  border-radius: 5px;
  font-size: 1.143rem;
  overflow: hidden;
`;

export const PanelHead = styled.figure`
  height: 160px;
  overflow: hidden;
`;

export const PanelContent = styled.div`
  padding: 1em;
  background-color: var(--color-elements);
  color: var(--color-text);
`;
